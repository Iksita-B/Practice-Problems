#include<stdio.h>
int main()
{
    char str1[100],str2[100];
    int len,i,j;
    scanf("%s",str1);
    for(len=0;str1[len]!='\0';len++);
    for(i=len-1,j=0;i>=0,j<len;i--,j++)
    {
        str2[j]=str1[i];
    }
    for(int k=0;k<len;k++)
    {
    printf("%c",str2[k]);
    }
}
